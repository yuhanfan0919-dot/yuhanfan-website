---
// V2 · Bold Statement — 极简宣言风
// 暖砂 #E8DCC7 + 锈红 #C44829 + Bricolage Grotesque 巨型大字
---

<!doctype html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>V2 · Bold · 范雨寒</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400..800&family=Manrope:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Noto+Sans+SC:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg: #E8DCC7;
        --ink: #1F1F1F;
        --muted: #6B6358;
        --accent: #C44829;
        --accent-dark: #8E3318;
        --paper: #F5ECDC;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }
      html { scroll-behavior: smooth; }
      body {
        background: var(--bg);
        color: var(--ink);
        font-family: 'Manrope', 'Noto Sans SC', system-ui, sans-serif;
        font-size: 16px;
        line-height: 1.6;
        -webkit-font-smoothing: antialiased;
        overflow-x: hidden;
      }

      /* Preview nav */
      .preview-nav {
        position: fixed; top: 1rem; right: 1rem; z-index: 50;
        background: var(--ink); color: var(--paper);
        border-radius: 9999px; padding: 0.375rem 0.5rem;
        display: flex; gap: 0.25rem; align-items: center;
        font-size: 0.75rem; font-family: 'JetBrains Mono', monospace;
      }
      .preview-nav a, .preview-nav span {
        color: var(--paper); text-decoration: none;
        opacity: 0.55;
        padding: 0.25rem 0.625rem; border-radius: 9999px;
        transition: all 0.2s;
      }
      .preview-nav a:hover { opacity: 1; background: var(--accent); }
      .preview-nav .current { opacity: 1; background: var(--accent); font-weight: 500; }

      .wrap { max-width: 1280px; margin: 0 auto; padding: 0 2rem; }

      /* Top status bar */
      .status-bar {
        padding: 1.5rem 0;
        display: flex; justify-content: space-between; align-items: center;
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem; color: var(--muted);
        text-transform: uppercase; letter-spacing: 0.15em;
      }
      .status-bar .dot {
        display: inline-block; width: 8px; height: 8px; border-radius: 50%;
        background: var(--accent); margin-right: 0.5rem;
        animation: pulse 2s ease-in-out infinite;
      }
      @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.4; }
      }

      /* HUGE HERO */
      .hero {
        padding: 4rem 0 6rem;
        position: relative;
      }
      .hero-statement {
        font-family: 'Bricolage Grotesque', 'Noto Sans SC', sans-serif;
        font-variation-settings: 'opsz' 96;
        font-weight: 600;
        font-size: clamp(3rem, 13vw, 11rem);
        line-height: 0.88;
        letter-spacing: -0.05em;
        margin-bottom: 2rem;
      }
      .hero-statement .accent {
        color: var(--accent);
        font-style: italic;
        font-weight: 700;
      }
      .hero-statement .strike {
        position: relative; color: var(--muted);
      }
      .hero-statement .strike::after {
        content: ""; position: absolute;
        left: 0; right: 0; top: 52%;
        height: 0.12em;
        background: var(--accent);
        transform: rotate(-3deg);
      }

      .hero-meta {
        display: grid;
        grid-template-columns: 1fr;
        gap: 2rem;
        margin-top: 4rem;
        max-width: 100%;
      }
      @media (min-width: 768px) {
        .hero-meta { grid-template-columns: 1fr 1fr; gap: 4rem; }
      }
      .hero-meta-block h3 {
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--accent);
        margin-bottom: 1rem;
      }
      .hero-meta-block p {
        font-size: 1.125rem;
        line-height: 1.7;
        color: var(--ink);
      }

      .hero-cta {
        margin-top: 4rem;
        display: flex; gap: 1rem; flex-wrap: wrap;
      }
      .btn {
        display: inline-flex; align-items: center; gap: 0.625rem;
        font-size: 0.9375rem; font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
      }
      .btn-primary {
        background: var(--ink); color: var(--bg);
        padding: 1.125rem 2rem; border-radius: 4px;
        font-family: 'Bricolage Grotesque', sans-serif;
      }
      .btn-primary:hover { background: var(--accent); transform: translate(-2px, -2px); box-shadow: 4px 4px 0 var(--ink); }
      .btn-ghost {
        color: var(--ink);
        padding: 1.125rem 2rem;
        border: 2px solid var(--ink);
        border-radius: 4px;
        font-family: 'Bricolage Grotesque', sans-serif;
      }
      .btn-ghost:hover { background: var(--ink); color: var(--bg); }

      /* About section */
      .section {
        padding: 6rem 0;
        border-top: 2px solid var(--ink);
      }
      .section-eyebrow {
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.25em;
        color: var(--accent);
        margin-bottom: 1.5rem;
      }
      .section-title {
        font-family: 'Bricolage Grotesque', 'Noto Sans SC', sans-serif;
        font-variation-settings: 'opsz' 48;
        font-size: clamp(2.5rem, 6vw, 4.5rem);
        font-weight: 600;
        line-height: 0.95;
        letter-spacing: -0.03em;
        margin-bottom: 3rem;
        max-width: 38rem;
      }
      .about-text p {
        font-size: 1.125rem; line-height: 1.75;
        margin-bottom: 1.5rem;
        max-width: 42rem;
      }
      .pullquote {
        font-family: 'Bricolage Grotesque', sans-serif;
        font-weight: 500;
        font-style: italic;
        font-size: clamp(1.5rem, 3vw, 2.25rem);
        line-height: 1.25;
        color: var(--accent);
        padding: 2rem 0;
        max-width: 42rem;
        border-top: 2px solid var(--accent);
        border-bottom: 2px solid var(--accent);
        margin: 3rem 0;
      }

      /* Footer */
      footer {
        padding: 3rem 0 4rem;
        border-top: 2px solid var(--ink);
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
        color: var(--muted);
        text-transform: uppercase; letter-spacing: 0.15em;
      }
    </style>
  </head>
  <body>
    <nav class="preview-nav">
      <a href="/preview/v1/">V1 Editorial</a>
      <span class="current">V2 Bold</span>
      <a href="/preview/v3/">V3 Soft</a>
      <a href="/preview/" title="back to index">↩</a>
    </nav>

    <div class="wrap">
      <header class="status-bar">
        <span><span class="dot"></span>AVAILABLE FOR AI ROLES</span>
        <span>RAIN · 范雨寒 · 2026</span>
      </header>

      <section class="hero">
        <h1 class="hero-statement">
          I make <span class="strike">spreadsheets</span><br />
          <span class="accent">agents</span>.
        </h1>

        <div class="hero-meta">
          <div class="hero-meta-block">
            <h3>谁</h3>
            <p>
              范雨寒 · Rain。西南财经财政学硕士，CPA，3 年 KPMG 审计。
              然后开始用 AI 改造财务流程，最近 5 周独立交付了一个生产级电商。
            </p>
          </div>
          <div class="hero-meta-block">
            <h3>在做什么</h3>
            <p>
              在找 AI 落地、Forward Deployed Engineer、AI 产品经理类岗位。
              我的角度是"懂业务的财务人 + 会用 AI 做事"。
            </p>
          </div>
        </div>

        <div class="hero-cta">
          <a class="btn btn-primary" href="#">看作品 →</a>
          <a class="btn btn-ghost" href="#">联系我</a>
        </div>
      </section>

      <section class="section">
        <p class="section-eyebrow">// 01 · About</p>
        <h2 class="section-title">
          从读懂公司，<br />到给公司做工具。
        </h2>

        <div class="about-text">
          <p>
            我的底盘是财务：西南财经财政学硕士、CPA、3 年 KPMG 深圳审计——做过 30+ 子公司双准则合并报表，主导过拟上市公司财务尽调。这段经历给了我两件事：看懂一家公司的能力，以及对"重复劳动"的高度厌恶。
          </p>
          <p>
            2024 年我离开 KPMG，去了一家短剧公司做财务 BP。第一次把 AI 引进多平台收入对账，效率 +30%。从那刻起我看清了——
          </p>
        </div>

        <blockquote class="pullquote">
          AI 真正解放的不是程序员，是那些懂业务但被流程困住的人。
        </blockquote>

        <div class="about-text">
          <p>
            今年我用 Cursor + Claude Code 在 5 周内交付了一个真实付费的电商系统。
            现在我想找的，是能把"懂业务的财务底子"和"会用 AI 做产品的手艺"结合起来的工作。
          </p>
        </div>
      </section>
    </div>

    <footer class="wrap">
      <p>V2 · Bold Statement — Sand #E8DCC7 · Rust #C44829 · Bricolage Grotesque / Manrope / JetBrains Mono</p>
    </footer>
  </body>
</html>
